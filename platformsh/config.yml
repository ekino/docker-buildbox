versions:
  "5":
    platforms:
      - linux/amd64
      - linux/arm64
    build_args:
      PLATFORM_VERSION: "5.0.6"
    test_config:
      cmd:
        - platform --version
        - python --version
        - pip --version
        - pipenv --version
