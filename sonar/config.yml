platforms: &platforms
  - linux/amd64
  # - linux/arm64

versions:
  "5.0":
    platforms: *platforms
    build_args:
      GLIBC_VERSION: 2.35-r1
      SONARSCANNER_VERSION: 5.0.1.3006
      ZLIB_VERSION: 1.2.11-3
    test_config:
      cmd:
        - java -version
        - sonar-scanner -v
