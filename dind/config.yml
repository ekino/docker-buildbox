versions:
  "1":
    test_config:
      cmd:
        - aws --version
        - az version
        - docker --version
        - docker-compose --version
        - task --version
        - trivy --version
