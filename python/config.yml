test_config: &test_config
  cmd:
    - pip --version
    - python --version
    - aws --version

versions:
  "3.7":
    test_config: *test_config
    platforms: *base_platforms
  "3.8":
    test_config: *test_config
    platforms: *base_platforms
  "3.9":
    test_config: *test_config
    platforms: *base_platforms
  "3.10":
    test_config: *test_config
    platforms: *base_platforms
