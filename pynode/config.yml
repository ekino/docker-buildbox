test_config: &test_config
  cmd:
    - pip --version
    - python --version
    - node --version
    - npm --version
    - aws --version

versions:
  "3.8":
    test_config: *test_config
    platforms: *base_platforms
  "3.9":
    test_config: *test_config
    platforms: *base_platforms
