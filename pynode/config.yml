test_config: &test_config
  cmd:
    - pip --version
    - python --version
    - node --version
    - npm --version
    - aws --version

versions:
  "3.8":
    platforms: 
      - linux/amd64
    test_config: *test_config
  "3.9":
    platforms: 
      - linux/amd64
    test_config: *test_config
