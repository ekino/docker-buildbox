versions:
  "5":
    platforms:
      - linux/amd64
      - linux/arm64
    github_versions:
      UV_VERSION: astral-sh/uv
    test_config:
      cmd:
        - prowler aws --list-checks
        - aws --version
        - uv --version
